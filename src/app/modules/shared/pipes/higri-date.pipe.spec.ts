import { HigriDatePipe } from './higri-date.pipe';

describe('HigriDatePipe', () => {
  it('create an instance', () => {
    const pipe = new HigriDatePipe();
    expect(pipe).toBeTruthy();
  });
});
